import { IdeaDescriptionPipe } from './idea-description.pipe';

describe('IdeaDescriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new IdeaDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
